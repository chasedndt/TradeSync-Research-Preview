fastapi==0.115.2
uvicorn[standard]==0.30.6
asyncpg==0.29.0
pydantic==2.9.2
python-dotenv==1.0.1
redis==5.0.0
httpx==0.27.2
# zoneinfo needs the IANA database; the slim image ships none.
tzdata==2024.1
# Web Push: P-256 ECDH and ES256 signing, and AES-128-GCM (app/web_push_encryption.py,
# app/web_push_jwt.py). The standard library has none of the three. The same version
# signer-svc already installs through eth-account; subtly wrong crypto is worse than none.
pycryptodome==3.23.0