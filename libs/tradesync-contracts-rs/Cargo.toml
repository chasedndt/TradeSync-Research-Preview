[package]
name = "tradesync-contracts"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Versioned cross-service contracts for TradeSync realtime and notification boundaries"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
